#include<stdio.h>

int main(){
    // FILE* fp = fopen("file.txt", "r+");
    // char buf[100] = {"hello world\nhi world"};

    // fputs(buf, fp);


    printf("%zu", sizeof("MYDB"));
    return 0;
}